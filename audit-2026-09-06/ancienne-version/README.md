# Ancienne version — le site tel qu'il était en ligne le 06/09/2026

Copie exacte des fichiers servis par https://hourdis.fonenako.mg/ (téléchargés en HTTP et par FTP en
lecture seule) avant l'audit : `index.html`, `src/js/main.js`, `src/styles/main.css`, les deux
scripts d'envoi jamais appelés (`contact.php`, `send_mail.php`) et le `.htaccess`. Les images sont
restées sur le serveur (`/image/`) jusqu'au nettoyage ; les originaux sont dans `sources-photos/`.

Retour arrière d'urgence (page blanche, ce que le déploiement atomique exclut) : téléverser ces
fichiers par FTP à la racine du site (`index.html` en dernier). Le formulaire de cette version
n'envoie rien : ce n'est pas une version « qui marche », c'est une preuve.
